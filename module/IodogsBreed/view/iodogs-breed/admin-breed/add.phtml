<div class="col-md-10 col-md-offset-1">
    <?php
    $editForm->setAttribute('action', $this->url('app/backoffice/breed/add'));
    echo $this->partial('partial/breed/forms/edit-form', array(
            'editForm' => $editForm,
            'viewArray' => $viewArray
        )
    );
    ?>
 </div>